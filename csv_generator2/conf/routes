# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /				            controllers.Application.index(messageToUserKey = "")

GET     /index:messageToUserKey     controllers.Application.index(messageToUserKey)
GET     /login		                controllers.Application.login
GET     /registration		        controllers.Registration.registration
POST    /register		        	controllers.Registration.register


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
