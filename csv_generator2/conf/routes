# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /				            controllers.Application.index(messageToUserKey = "")

GET     /index:messageToUserKey     controllers.Application.index(messageToUserKey)
GET     /loginForm	                controllers.Login.loginForm
GET     /login                		controllers.Login.login
GET     /registrationForm		    controllers.Registration.registrationForm
POST    /registration		        controllers.Registration.registration


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
