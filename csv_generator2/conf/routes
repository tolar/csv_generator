# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /				            controllers.Application.index(messageToUser = "")

GET     /index:messageToUser        controllers.Application.index(messageToUser)
GET     /login		                controllers.Application.login
GET     /registration		        controllers.Application.registration
POST    /register		        	controllers.Application.register
GET     /:messageToUser             controllers.Application.index(messageToUser)



# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
